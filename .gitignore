# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Next.js
.next
.next/
.next.stale-*
.next-build
.next-stale-*
out
# Auto-generated by `next dev`/`next build`; flip-flops between dev/build paths.
# Next.js officially recommends gitignoring it (regenerated on every build).
next-env.d.ts

# Scratch screenshots / artifacts dropped by agents (chrome-devtools etc.)
.tmp/
.tmp-*/
/tmp/
# Scratch image / diff artifacts dropped by agents (playwright comparisons etc.)
/_*.png
# Playwright MCP scratch logs (console/page dumps)
/.playwright-mcp/

# Design-sync: converter scripts + generated upload bundle (local tooling only)
.ds-sync/
ds-bundle/

# Impeccable: local design-review/critique notes
.impeccable/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local
.env.*.local
.vercel

# Generated sheet visualizations
sheets/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
tests/e2e/screenshots/*.png
!tests/e2e/screenshots/.gitkeep
data/screenshots/*.png
data/auth-state.json
.leebo/screenshots/*.png

# Test results (generated by postbuild, regenerated on every deploy)
src/tests/results.json
src/qa/results.json
public/test-results.json
public/qa-results.json
public/qa-status.json
public/RELEASE_NOTES.md
docs/QA_STATUS.md
src/data/card-fixes-report.json
backup_*.sql
migrations/.buildstamp

# Database backups
.backups/

# Git worktrees
.worktrees/
.claude/settings.local.json
.claude/launch.json

# Import Pool: synthetic-only fixtures; never commit real player photos (per memory rule)
tests/fixtures/import-pool/
scripts/eval/results/*.json

# Import Pool eval: photos contain real player names + SWU IDs, kept local only
scripts/eval/fixtures/*/photo*.jpg
scripts/eval/fixtures/*/photo*.jpeg
scripts/eval/fixtures/*/photo*.png
scripts/eval/heic-ab/
scripts/eval/pipeline-runs/
scripts/omr/__pycache__/
tests/e2e/crop-verify-screenshots/

# DNS snapshots from scripts/dns-snapshot.sh (transient, for migration diffs)
dns-snapshot-*.txt
dns-squarespace.txt
dns-cloudflare.txt

# BIND zone files generated for one-off Cloudflare imports
*.zone
.gstack/
.worktrees

# design-sync (claude.ai/design): staged converter, build output, machine state.
# Durable inputs (design-sync.config.json, .design-sync/{NOTES.md,entry.tsx,previews,overrides}) ARE committed.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules/

# temp verification artifacts (screenshots + throwaway specs)
_verify-*
**/_verify-*
.env.fable-bak
.next-promo-e2e
