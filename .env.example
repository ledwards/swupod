# ============================================
# Protect the Pod - Environment Variables
# ============================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control (it's in .gitignore)

# ============================================
# Database Configuration
# ============================================
# PostgreSQL connection string
# Get from: Vercel Dashboard → Storage → Your Database → Connection String
# Format: postgresql://user:password@host:port/database
POSTGRES_URL=postgresql://user:password@localhost:5432/protectthepod

# Optional: Separate development database URL
# If not set, POSTGRES_URL will be used for both dev and prod
# POSTGRES_URL_DEV=postgresql://user:password@dev-host:5432/protectthepod_dev

# Legacy alias (some tools may use this)
# DATABASE_URL=postgresql://user:password@localhost:5432/protectthepod

# ============================================
# Application Configuration
# ============================================
# Base URL of your application
# For local development: http://localhost:3000 (or port Vercel uses)
# For production: https://your-domain.vercel.app
APP_URL=http://localhost:3000

# ============================================
# Discord OAuth Configuration
# ============================================
# Get these from: https://discord.com/developers/applications
# 1. Create a new application
# 2. Go to OAuth2 → General
# 3. Copy Client ID and Client Secret
# 4. Add redirect URI: ${APP_URL}/api/auth/callback/discord
DISCORD_CLIENT_ID=your_discord_client_id_here
DISCORD_CLIENT_SECRET=your_discord_client_secret_here

# ============================================
# Discord Bot (Beta Access Check)
# ============================================
# Used to check if a user has the "Friend of the Pod" role in Discord.
# Setup:
#   1. In Discord Developer Portal, enable the Bot section for your app
#   2. Enable "Server Members Intent" under Bot → Privileged Gateway Intents
#   3. Invite bot to server with guilds.members.read permission
#   4. Copy Bot Token from Bot section
#   5. Get Guild ID: enable Developer Mode in Discord, right-click server → Copy ID
#   6. Get Role ID: Server Settings → Roles → right-click role → Copy ID
DISCORD_BOT_TOKEN=your_discord_bot_token_here
DISCORD_GUILD_ID=your_discord_server_id_here
DISCORD_FRIEND_OF_THE_POD_ROLE_ID=your_role_id_here
DISCORD_BETA_TESTER_ROLE_ID=your_beta_tester_role_id_here

# ============================================
# Discord LFG Channels (Pod Chat)
# ============================================
# Channel IDs for #draft-now and #sealed-now LFG channels
# Public pods are announced here and threads are created for pod chat
DISCORD_DRAFT_NOW_CHANNEL_ID=your_draft_now_channel_id_here
DISCORD_SEALED_NOW_CHANNEL_ID=your_sealed_now_channel_id_here

# Channel ID for #draftbots (bot deck summaries posted after drafts)
# Dev: 1482545144858411008, Prod: 1482545251137880065
DISCORD_DRAFTBOTS_CHANNEL_ID=1482545144858411008

# Channel ID for pool discussion (user deck posts)
# Dev: 1482611622920065204, Prod: 1482611447661068379
DISCORD_POOL_DISCUSSION_CHANNEL_ID=1482611622920065204

# Discord server invite URL (used for "Join Discord" buttons)
NEXT_PUBLIC_DISCORD_INVITE_URL=

# Shared secret for authenticating chat relay from Leebo bot
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CHAT_RELAY_SECRET=your_chat_relay_secret_here

# ============================================
# Authentication & Security
# ============================================
# JWT secret for signing authentication tokens
# Generate a secure random string with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# IMPORTANT: Use a different secret for production!
JWT_SECRET=change-me-to-a-random-secret-string

# Alternative: NextAuth secret (if using NextAuth.js)
# NEXTAUTH_SECRET=change-me-to-a-random-secret-string

# ============================================
# Optional: Google OAuth (Not Yet Implemented)
# ============================================
# Uncomment when Google OAuth is implemented
# GOOGLE_CLIENT_ID=your_google_client_id
# GOOGLE_CLIENT_SECRET=your_google_client_secret

# ============================================
# Optional: API Configuration
# ============================================
# Custom API base URL (if different from APP_URL)
# VITE_API_URL=/api

NEXT_PUBLIC_SITE_URL=https://www.protectthepod.com
# Stats page filter - exclude data before position-based tracking was fixed
# Set to the date when proper slot_type tracking was deployed
NEXT_PUBLIC_STATS_START_DATE=2026-02-12

# ============================================
# Analytics (PostHog)
# ============================================
# Get from: https://app.posthog.com → Project Settings → Project API Key
# Leave blank to disable analytics
NEXT_PUBLIC_POSTHOG_KEY=phc_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# ============================================
# Patreon Webhook (Patron Role Backstop)
# ============================================
# Webhook secret for verifying Patreon webhook signatures (HMAC-MD5).
# Setup:
#   1. Go to https://www.patreon.com/portal/registration/register-webhooks
#   2. Add webhook URL: https://your-domain.com/api/webhooks/patreon
#   3. Select triggers: members:pledge:create, members:pledge:update, members:pledge:delete
#   4. Copy the webhook secret Patreon generates
PATREON_WEBHOOK_SECRET=your_patreon_webhook_secret_here

# Patreon API (for patron sync - fetching all patrons to assign Discord roles)
# Get from: https://www.patreon.com/portal/registration/register-clients
# Creator access token: Settings > Developers > Create API Client > Creator's Access Token
# Campaign ID: GET https://www.patreon.com/api/oauth2/v2/campaigns (with your token)
PATREON_CREATOR_ACCESS_TOKEN=your_patreon_creator_access_token_here
PATREON_CAMPAIGN_ID=your_patreon_campaign_id_here

# ============================================
# Private API (Server-to-Server Integration)
# ============================================
# Service key for authenticated server-to-server API calls.
# Used by SWUTeam to fetch per-user data via GET /api/private/user-data.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# The calling service sends this as: Authorization: Bearer <PTP_SERVICE_KEY>
PTP_SERVICE_KEY=

# ============================================
# SWUAPI (Card Image Generation)
# ============================================
SWUAPI_URL=https://api.swuapi.com
